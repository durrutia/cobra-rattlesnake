# Cobra RattleSnake

A small fictional Jekyll site for a Taiwanese-inspired hot-sauce company with a cycling theme.

## Run locally

```bash
bundle install
bundle exec jekyll serve
```

Then open http://localhost:4000.

The site intentionally uses no Jekyll theme gem: the visual design is contained in `assets/css/style.css`, making it easy to customize.
# cobra-rattlesnake
